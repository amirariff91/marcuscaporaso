Read feature-research/ergoworks-review-site/BUILD-SPEC.md IN FULL and follow every rule exactly, especially the REDACTION POLICY (mandatory) and the "preserve compliance copy verbatim" rules. Also read src/app/ergoworks/campaign-plan/page.tsx and src/app/ergoworks/plan/page.tsx to copy the exact JSX idiom, the SectionHeading helper, and how classes from src/app/ergoworks/plan/plan.module.css are used. This is a Next.js 16 server component (NO "use client"). Import styles from "../plan.module.css". Do NOT add a cross-page nav or .sectionNav (the layout provides them). Do NOT import or fs-read anything under docs/ — transcribe redacted content as hard-coded literals. Export metadata with robots:{index:false,follow:false}.

TASK: Build the ACCOUNT AUDIT page (Appendix E).
Source: docs/ergoworks-consulting/appendices/E-existing-account-audit.md
Target: src/app/ergoworks/plan/audit/page.tsx
Cover E1-E8: account baseline & audit boundaries (ABSTRACT all IDs/labels/A$ — "the Ads account", "the GA4 property", "the Search campaign", "a paused PMax campaign", "a paused remarketing campaign"; keep percentages like the 49% concentration and 18.7%), repair the conversion objective, bidding assessment & decision gate, spend concentration & conditional rebalancing (3x Kill Rule / 20% Scaling Rule — describe the RULES, drop the dollar bases), brand ad-group zero-delivery diagnosis, paused PMax decision, paused remarketing rebuild, diagnostic work plan. Use .prose for narrative and .tableScroll tables for the matrices. NO LaTeX/dollar CPA calcs — replace with qualitative statements. Preserve compliance caveats verbatim.
After writing, print a short list of every redaction substitution you made.
