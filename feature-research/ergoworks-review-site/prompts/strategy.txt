Read feature-research/ergoworks-review-site/BUILD-SPEC.md IN FULL and follow every rule exactly, especially the REDACTION POLICY (mandatory) and the "preserve compliance copy verbatim" rules. Also read src/app/ergoworks/campaign-plan/page.tsx and src/app/ergoworks/plan/page.tsx to copy the exact JSX idiom, the SectionHeading helper, and how classes from src/app/ergoworks/plan/plan.module.css are used. This is a Next.js 16 server component (NO "use client"). Import styles from "../plan.module.css". Do NOT add a cross-page nav or .sectionNav (the layout provides them). Do NOT import or fs-read anything under docs/ — transcribe redacted content as hard-coded literals. Export metadata with robots:{index:false,follow:false}.

TASK: Build the STRATEGY page.
Source: docs/ergoworks-consulting/ergoworks-campaign-plan.md
Target: src/app/ergoworks/plan/strategy/page.tsx
Cover, in order: executive summary, the big idea, current-state snapshot (ABSTRACT the verified account baseline — no IDs, no A$ figures; keep ratios/percentages and qualitative structure), offer & positioning (offer registry), channel & budget at a glance (use the 70/20/10 allocation bars like the reference; budget scenarios as a .tableScroll table with RELATIVE labels only, no dollars), landing-page concept, measurement approach, 30/60/90 roadmap (use the .roadmap 3-col grid), risks & compliance gates (use .gatesList), open decisions (brief — full list is the Decisions page), and the "Not assessed / data required" list. Keep every Snapshot caveat adjacent to Snapshot mentions.
After writing, print a short list of every redaction substitution you made.
