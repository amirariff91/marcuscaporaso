Read feature-research/ergoworks-review-site/BUILD-SPEC.md IN FULL and follow every rule exactly, especially the REDACTION POLICY (mandatory) and the "preserve compliance copy verbatim" rules. Also read src/app/ergoworks/campaign-plan/page.tsx and src/app/ergoworks/plan/page.tsx to copy the exact JSX idiom, the SectionHeading helper, and how classes from src/app/ergoworks/plan/plan.module.css are used. This is a Next.js 16 server component (NO "use client"). Import styles from "../plan.module.css". Do NOT add a cross-page nav or .sectionNav (the layout provides them). Do NOT import or fs-read anything under docs/ — transcribe redacted content as hard-coded literals. Export metadata with robots:{index:false,follow:false}.

TASK: Build the LANDING PAGE page (Appendix A) — the longest source.
Source: docs/ergoworks-consulting/appendices/A-landing-page-copy.md
Target: src/app/ergoworks/plan/landing-page/page.tsx
Cover: purpose & conversion objective, modular wireframe + page-level rules, the 3 variants (Manual Handling Training; Workstation/Ergonomic Assessment; Hybrid/Home-office) — summarise each variant's hero, proof strip, benefit blocks, gated modules, how-it-works, FAQ, final CTA (do NOT paste every word; give an executive digest with representative copy blocks), the two-step form spec (Step 1 contact / Step 2 qualification / consent controls / validation / thank-you) as .tableScroll tables, CTA language options, message-match table, mobile & performance criteria, measurement & attribution (event design), testing order, pre-launch acceptance checklist. Use the .lpMock browser mock from the reference page for the hero visual. Named proof (Danone/Telstra/Nokia/ASX 100/5.0-from-63) -> abstract per policy but KEEP "client sign-off required". NO A$ figures. Preserve every "preliminary guidance only..." caveat and the "never imply a health condition / no injury-history fields" rules verbatim.
After writing, print a short list of every redaction substitution you made.
